#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: lnumber of command line argument
 * @argv: array of the command line argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);

	return (0);
}
